project('zennote', 'c',
          version: '0.1.0',
    meson_version: '>= 1.0.0',
  default_options: [ 'warning_level=2', 'werror=false', ],
)
i18n = import('i18n')
gnome = import('gnome')

subdir('zennote')

gnome.post_install(
    glib_compile_schemas: true,
)